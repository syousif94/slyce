import { ViewStyle } from 'react-native';

const ToggleControlStyle: ViewStyle = {
  flexDirection: 'row',
  alignItems: 'center',
  paddingVertical: 4,
  paddingHorizontal: 10,
};

export default ToggleControlStyle;
